[project]
name = "drmlke-api"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "drmlke-core",
  "fastapi",
  "uvicorn[standard]",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
