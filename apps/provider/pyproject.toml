[project]
name = "drmlke-provider"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "drmlke-core",
  "fastapi",
  "uvicorn[standard]",
]

[project.scripts]
drmlke-provider = "drmlke_provider.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
