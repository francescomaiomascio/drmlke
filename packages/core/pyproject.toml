[project]
name = "drmlke-core"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "pydantic",
  "pydantic-settings",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
